<% if @feed_items.any? %>
  <ol class="orders">
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items, params: { controller: :static_pages, action: :home }, class: "center" %>
<% end %>